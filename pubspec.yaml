name: earnitest
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  bloc: ^8.1.2
  dartz: ^0.10.1
  equatable: ^2.0.5
  fast_cached_network_image: ^1.2.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_staggered_grid_view: ^0.6.2
  get_it: ^7.6.0
  http: ^1.0.0
  internet_connection_checker: ^1.0.0+1
  shared_preferences: ^2.1.2

dev_dependencies:
  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^2.0.0
  flutter_native_splash: ^2.3.1
  flutter_test:
    sdk: flutter
  mockito: ^5.4.2
  # mocktail: ^0.3.0

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/leaf.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true

  assets:
    - assets/images/
